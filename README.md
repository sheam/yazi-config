# YAZI CONFIG FILES

## Quick Start
- cd `~/.config`
- `git clone git@github.com:sheam/yazi-config.git yazi`
- note the `yazi` at the end of the clone command, it needs to be cloned into the nvim directory
